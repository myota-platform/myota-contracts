openapi: 3.1.0
info:
  title: MyOTA Outdoor Activation Platform APIs
  version: 1.0.0
  description: Service contracts for a programme-agnostic amateur-radio outdoor activation platform.
servers:
  - url: http://localhost:8080
tags:
  - name: Identity
  - name: Programmes
  - name: Geodata
  - name: Activity
  - name: Awards
paths:
  /v1/identity/auth/register:
    post:
      tags: [Identity]
      summary: Register an operator or SWL and create a session
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/RegisterAccount' } } }
      responses: { '201': { description: Registered and authenticated } }
  /v1/identity/auth/login:
    post:
      tags: [Identity]
      summary: Authenticate with email and password
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/Login' } } }
      responses: { '200': { description: Authenticated } }
  /v1/identity/auth/refresh:
    post:
      tags: [Identity]
      summary: Rotate a refresh token
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/RefreshToken' } } }
      responses: { '200': { description: Rotated session } }
  /v1/identity/auth/logout:
    post:
      tags: [Identity]
      summary: Revoke a session or access token
      security: [{ bearerAuth: [] }]
      requestBody:
        content: { application/json: { schema: { $ref: '#/components/schemas/RefreshToken' } } }
      responses: { '200': { description: Logged out } }
  /v1/identity/auth/recovery/request:
    post:
      tags: [Identity]
      summary: Request password recovery without disclosing account existence
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/RecoveryRequest' } } }
      responses: { '200': { description: Recovery request accepted } }
  /v1/identity/auth/recovery/reset:
    post:
      tags: [Identity]
      summary: Set a new password using a single-use recovery token
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/RecoveryReset' } } }
      responses: { '200': { description: Password reset } }
  /v1/identity/me:
    get:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      summary: Get the authenticated account and scopes
      responses: { '200': { description: Authenticated account } }
  /v1/identity/admin/accounts:
    get:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      summary: List accounts for global identity administrators
      parameters:
        - { in: query, name: q, schema: { type: string } }
        - { in: query, name: status, schema: { type: string } }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses: { '200': { description: Account page } }
  /v1/identity/admin/accounts/{accountId}/update:
    post:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      summary: Edit an account and replace its active administrative roles
      parameters: [{ $ref: '#/components/parameters/AccountId' }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                displayName: { type: string }
                email: { type: string, format: email, nullable: true }
                participationType: { type: string, enum: [OPERATOR, SWL] }
                status: { type: string, enum: [ACTIVE, DEACTIVATED] }
                password: { type: string, minLength: 12, writeOnly: true }
                roles:
                  type: array
                  items: { $ref: '#/components/schemas/RoleAssignment' }
      responses: { '200': { description: Updated account and active roles } }
  /v1/identity/admin/roles:
    get:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      summary: List built-in and custom administrative roles and permissions
      responses: { '200': { description: Role catalogue } }
    post:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      summary: Create a custom administrative role
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AdminRoleDefinition' } } }
      responses: { '201': { description: Created role definition } }
  /v1/identity/admin/roles/{roleCode}/update:
    post:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      summary: Edit a custom administrative role
      parameters:
        - in: path
          name: roleCode
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AdminRoleDefinition' } } }
      responses: { '200': { description: Updated role definition } }
  /v1/identity/admin/security-events:
    get:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      summary: List security events for global identity administrators
      parameters:
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses: { '200': { description: Security-event page } }
  /v1/identity/accounts:
    post:
      tags: [Identity]
      summary: Create an operator or SWL account
      operationId: createAccount
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateAccount' }
      responses:
        '201':
          description: Created
          content: { application/json: { schema: { $ref: '#/components/schemas/Account' } } }
  /v1/identity/accounts/{accountId}/callsigns:
    post:
      tags: [Identity]
      summary: Add and optionally verify a callsign
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AddCallsign' }
      responses:
        '201': { description: Created }
  /v1/identity/accounts/{accountId}/callsigns/{callsignId}/evidence:
    post:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      summary: Submit callsign verification evidence
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/CallsignId'
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/CallsignEvidence' } } }
      responses: { '201': { description: Evidence submitted } }
  /v1/identity/accounts/{accountId}/callsigns/{callsignId}/verify:
    post:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      summary: Verify callsign evidence using the callsign.verify scope
      parameters:
        - $ref: '#/components/parameters/AccountId'
        - $ref: '#/components/parameters/CallsignId'
      responses: { '200': { description: Callsign verified } }
  /v1/identity/accounts/{accountId}/roles:
    get:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/AccountId' }]
      responses: { '200': { description: Account roles } }
    post:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/AccountId' }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/RoleAssignment' } } }
      responses: { '201': { description: Role assigned } }
  /v1/identity/accounts/{accountId}/export:
    get:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/AccountId' }]
      responses: { '200': { description: Personal data export } }
  /v1/identity/accounts/{accountId}/deactivate:
    post:
      tags: [Identity]
      security: [{ bearerAuth: [] }]
      parameters: [{ $ref: '#/components/parameters/AccountId' }]
      responses: { '200': { description: Account deactivated and sessions revoked } }
  /v1/programmes:
    get:
      tags: [Programmes]
      summary: List configured programmes
      responses:
        '200': { description: Programme list }
    post:
      tags: [Programmes]
      summary: Create a programme configuration
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/Programme' }
      responses: { '201': { description: Created } }
  /v1/programmes/{slug}/policy:
    get:
      tags: [Programmes]
      summary: Get the effective programme policy version
      parameters:
        - in: path
          name: slug
          required: true
          schema: { type: string }
      responses:
        '200': { description: Effective programme policy }
  /v1/programmes/{slug}/update:
    post:
      tags: [Programmes]
      security: [{ bearerAuth: [] }]
      summary: Update programme-owned configuration
      parameters:
        - in: path
          name: slug
          required: true
          schema: { type: string }
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        content: { application/json: { schema: { $ref: '#/components/schemas/ProgrammeUpdate' } } }
      responses: { '200': { description: Updated programme } }
  /v1/programmes/{slug}/archive:
    post:
      tags: [Programmes]
      security: [{ bearerAuth: [] }]
      summary: Archive a programme
      parameters:
        - in: path
          name: slug
          required: true
          schema: { type: string }
        - $ref: '#/components/parameters/IdempotencyKey'
      responses: { '200': { description: Archived programme } }
  /v1/geodata/entities:
    get:
      tags: [Geodata]
      summary: List verified and candidate entities
      parameters:
        - in: query
          name: programme
          schema: { type: string }
        - in: query
          name: status
          schema: { type: string, enum: [CANDIDATE, PROPOSED, APPROVED, REJECTED] }
        - { in: query, name: minLon, schema: { type: number, format: double } }
        - { in: query, name: minLat, schema: { type: number, format: double } }
        - { in: query, name: maxLon, schema: { type: number, format: double } }
        - { in: query, name: maxLat, schema: { type: number, format: double } }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses:
        '200':
          description: Entity list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items: { $ref: '#/components/schemas/GeoEntity' }
                  total: { type: integer }
                  page: { type: integer }
                  pageSize: { type: integer }
                  nextPage: { type: integer, nullable: true }
  /v1/geodata/imports/manual:
    post:
      tags: [Geodata]
      summary: Import GeoJSON features as candidates
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: '#/components/schemas/GeoImport' } }
      responses: { '202': { description: Import accepted } }
  /v1/geodata/location-options:
    get:
      tags: [Geodata]
      security: [{ bearerAuth: [] }]
      summary: List provider-derived location hierarchy options
      description: Values are aggregated from stored BigDataCloud reverse-geocoding responses; this endpoint does not issue remote lookups.
      responses:
        '200':
          description: Searchable continent, country, subdivision, and province hierarchy
          content: { application/json: { schema: { $ref: '#/components/schemas/LocationOptions' } } }
  /v1/geodata/entities/{entityId}/review:
    post:
      tags: [Geodata]
      summary: Review a proposed entity
      parameters:
        - $ref: '#/components/parameters/EntityId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [decision, reviewerId]
              properties:
                decision: { type: string, enum: [APPROVED, REJECTED] }
                reviewerId: { type: string }
                note: { type: string }
      responses: { '200': { description: Review recorded } }
  /v1/geodata/entities/{entityId}/geometry-type:
    post:
      tags: [Geodata]
      security: [{ bearerAuth: [] }]
      summary: Convert a geodata entity between point, way, and polygon geometry
      parameters:
        - $ref: '#/components/parameters/EntityId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [geometryType, editorId]
              properties:
                geometryType: { type: string, enum: [POINT, WAY, POLYGON] }
                editorId: { type: string }
                note: { type: string }
      responses: { '200': { description: Geometry type changed and audited } }
  /v1/geodata/entities/{entityId}/location:
    post:
      tags: [Geodata]
      security: [{ bearerAuth: [] }]
      summary: Save manual location metadata and its provider precedence
      parameters:
        - $ref: '#/components/parameters/EntityId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: '#/components/schemas/LocationMetadataEdit' } }
      responses: { '200': { description: Location metadata saved and audited } }
  /v1/geodata/entities/{entityId}/delete:
    post:
      tags: [Geodata]
      security: [{ bearerAuth: [] }]
      summary: Permanently delete a rejected entity and its audit record
      parameters:
        - $ref: '#/components/parameters/EntityId'
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content: { application/json: { schema: { type: object, required: [deletedBy], properties: { deletedBy: { type: string } } } } }
      responses: { '204': { description: Rejected entity and audit record deleted } }
  /v1/activations:
    get:
      tags: [Activity]
      security: [{ bearerAuth: [] }]
      summary: List activations for authorised operators and administrators
      parameters:
        - in: query
          name: programme
          schema: { type: string }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses: { '200': { description: Activation page } }
    post:
      tags: [Activity]
      summary: Start an activation
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/Activation' } } }
      responses: { '201': { description: Created } }
  /v1/activations/{activationId}:
    get:
      tags: [Activity]
      security: [{ bearerAuth: [] }]
      summary: Get an activation and its normalized QSOs
      parameters: [{ $ref: '#/components/parameters/ActivationId' }]
      responses: { '200': { description: Activation } }
  /v1/activations/{activationId}/qsos:
    post:
      tags: [Activity]
      security: [{ bearerAuth: [] }]
      summary: Record a normalized QSO for an open activation
      parameters: [{ $ref: '#/components/parameters/ActivationId' }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/Qso' } } }
      responses: { '201': { description: QSO recorded } }
  /v1/activations/{activationId}/close:
    post:
      tags: [Activity]
      security: [{ bearerAuth: [] }]
      summary: Close an activation
      parameters: [{ $ref: '#/components/parameters/ActivationId' }]
      responses: { '200': { description: Activation closed } }
  /v1/awards:
    get:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: List programme-owned award definitions
      parameters:
        - { in: query, name: programme, schema: { type: string } }
        - { in: query, name: category, schema: { type: string, enum: [HUNTER, ACTIVATOR] } }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses: { '200': { description: Award definition page } }
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Save a draft programme-owned award definition
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AwardDefinition' } } }
      responses: { '201': { description: Draft award created } }
  /v1/awards/{awardId}:
    get:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Get an award definition
      parameters: [{ $ref: '#/components/parameters/AwardId' }]
      responses: { '200': { description: Award definition } }
  /v1/awards/{awardId}/submit:
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Submit an award definition for review
      parameters: [{ $ref: '#/components/parameters/AwardId' }]
      responses: { '200': { description: Award submitted } }
  /v1/awards/{awardId}/review:
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Approve or return an award definition for changes
      parameters: [{ $ref: '#/components/parameters/AwardId' }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AwardReview' } } }
      responses: { '200': { description: Review recorded } }
  /v1/awards/{awardId}/publish:
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Publish an approved award with an effective date
      parameters: [{ $ref: '#/components/parameters/AwardId' }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AwardPublication' } } }
      responses: { '200': { description: Award published } }
  /v1/awards/assets:
    get:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: List registered background and signature assets
      responses: { '200': { description: Asset page } }
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Register an image asset stored in S3-compatible object storage
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AwardAsset' } } }
      responses: { '201': { description: Asset registered } }
  /v1/awards/assets/{assetId}/upload-url:
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Create a short-lived direct upload URL for an image asset
      parameters: [{ $ref: '#/components/parameters/AssetId' }]
      responses: { '200': { description: Presigned object upload URL } }
  /v1/awards/assets/{assetId}/content:
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Upload small image content through the API
      parameters: [{ $ref: '#/components/parameters/AssetId' }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AssetContent' } } }
      responses: { '200': { description: Stored asset metadata } }
  /v1/awards/evaluate:
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Evaluate award conditions and eligible levels for a subject
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AwardEvaluationRequest' } } }
      responses: { '200': { description: Evaluation result } }
  /v1/awards/progress:
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Calculate server-side award progress for the authenticated participant
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AwardProgressRequest' } } }
      responses: { '200': { description: Progress and eligible levels } }
  /v1/awards/requests:
    get:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: List award requests
      parameters:
        - { in: query, name: subjectId, schema: { type: string, format: uuid } }
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/PageSize'
      responses: { '200': { description: Award request page } }
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Request an eligible award level
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AwardRequest' } } }
      responses: { '201': { description: Award request created } }
  /v1/awards/requests/{requestId}/issue:
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Issue and permanently record a certificate request
      parameters: [{ $ref: '#/components/parameters/RequestId' }]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: '#/components/schemas/AwardIssuanceRequest' } } }
      responses: { '201': { description: Award issuance created } }
  /v1/awards/issuances:
    get:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: List permanent award issuance records
      responses: { '200': { description: Award issuance page } }
  /v1/awards/issuances/{issuanceId}/render:
    post:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Retry rendering an issued certificate after its assets are available
      parameters: [{ $ref: '#/components/parameters/IssuanceId' }]
      responses: { '200': { description: Certificate rendered } }
  /v1/awards/issuances/{issuanceId}/download:
    get:
      tags: [Awards]
      security: [{ bearerAuth: [] }]
      summary: Get a short-lived certificate download URL
      parameters: [{ $ref: '#/components/parameters/IssuanceId' }]
      responses: { '200': { description: Presigned certificate download URL } }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    IdempotencyKey:
      in: header
      name: Idempotency-Key
      required: false
      schema: { type: string, maxLength: 128 }
    AccountId:
      in: path
      name: accountId
      required: true
      schema: { type: string, format: uuid }
    EntityId:
      in: path
      name: entityId
      required: true
      schema: { type: string, format: uuid }
    ActivationId:
      in: path
      name: activationId
      required: true
      schema: { type: string, format: uuid }
    CallsignId:
      in: path
      name: callsignId
      required: true
      schema: { type: string, format: uuid }
    AwardId:
      in: path
      name: awardId
      required: true
      schema: { type: string, format: uuid }
    RequestId:
      in: path
      name: requestId
      required: true
      schema: { type: string, format: uuid }
    AssetId:
      in: path
      name: assetId
      required: true
      schema: { type: string, format: uuid }
    IssuanceId:
      in: path
      name: issuanceId
      required: true
      schema: { type: string, format: uuid }
    Page:
      in: query
      name: page
      schema: { type: integer, minimum: 1, default: 1 }
    PageSize:
      in: query
      name: pageSize
      schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
  schemas:
    Account:
      type: object
      required: [id, displayName, participationType, status]
      properties:
        id: { type: string, format: uuid }
        displayName: { type: string }
        email: { type: string, format: email, nullable: true }
        participationType: { type: string, enum: [OPERATOR, SWL] }
        status: { type: string, enum: [ACTIVE, DEACTIVATED] }
        callsigns: { type: array, items: { type: object } }
    RegisterAccount:
      allOf:
        - $ref: '#/components/schemas/CreateAccount'
        - type: object
          required: [email, password]
          properties:
            password: { type: string, minLength: 12, format: password }
    Login:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email }
        password: { type: string, format: password }
    RefreshToken:
      type: object
      required: [refreshToken]
      properties: { refreshToken: { type: string } }
    RecoveryRequest:
      type: object
      required: [email]
      properties: { email: { type: string, format: email } }
    RecoveryReset:
      type: object
      required: [token, newPassword]
      properties:
        token: { type: string }
        newPassword: { type: string, minLength: 12, format: password }
    CallsignEvidence:
      type: object
      required: [evidenceType, source]
      properties:
        evidenceType: { type: string }
        source: { type: string }
        checksum: { type: string }
        metadata: { type: object }
    RoleAssignment:
      type: object
      required: [role]
      properties:
        role: { type: string }
        code: { type: string, description: Role-definition code accepted by administrative role replacement }
        programmeSlug: { type: string }
        jurisdiction: { type: string }
        entityType: { type: string }
        scopes: { type: array, items: { type: string } }
    AdminRoleDefinition:
      type: object
      required: [code, name]
      properties:
        code: { type: string, pattern: '^[A-Z][A-Z0-9_]*$' }
        name: { type: string }
        description: { type: string }
        scopes: { type: array, uniqueItems: true, items: { type: string } }
    CreateAccount:
      type: object
      required: [displayName, participationType]
      properties:
        displayName: { type: string, minLength: 1 }
        email: { type: string, format: email }
        participationType: { type: string, enum: [OPERATOR, SWL] }
        callsign: { type: string, description: Optional initial callsign for an operator }
    AddCallsign:
      type: object
      required: [callsign]
      properties:
        callsign: { type: string }
        status: { type: string, enum: [UNVERIFIED, VERIFIED, RETIRED] }
        source: { type: string }
    Programme:
      type: object
      required: [slug, name, entityTypes, rules]
      properties:
        slug: { type: string }
        name: { type: string }
        entityTypes: { type: array, items: { type: object } }
        rules: { type: object }
        theme: { type: object }
        oidc: { type: object, nullable: true }
    ProgrammeUpdate:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        entityTypes: { type: array, items: { type: object } }
        rules: { type: object }
        theme: { type: object }
        oidc: { type: object, nullable: true }
    GeoImport:
      type: object
      required: [programmeSlug, adapter, source, features]
      properties:
        programmeSlug: { type: string }
        adapter: { type: string, enum: [PARKSERVE_US, OSM, GOVERNMENT_GIS, MANUAL] }
        source: { type: object }
        features: { type: array, items: { type: object } }
    GeoEntity:
      type: object
      required: [id, programmeSlug, name, status, geometry]
      properties:
        id: { type: string, format: uuid }
        programmeSlug: { type: string }
        entityType: { type: string }
        name: { type: string }
        status: { type: string, enum: [CANDIDATE, PROPOSED, APPROVED, REJECTED, RETIRED] }
        geometry: { type: object, additionalProperties: true, description: GeoJSON geometry. Ways use type LineString; the import API also accepts the non-standard geometry type alias way and normalizes it to LineString. }
        centroid: { type: object, additionalProperties: true }
        continent: { type: string, nullable: true }
        continentCode: { type: string, nullable: true, description: ISO-style continent code from the geocoder }
        country: { type: string, nullable: true }
        countryCode: { type: string, nullable: true, description: ISO 3166-1 country code }
        region: { type: string, nullable: true, description: First administrative subdivision after the country }
        regionCode: { type: string, nullable: true, description: ISO 3166-2 code for the first subdivision after the country }
        subdivision: { type: string, nullable: true }
        subdivisionCode: { type: string, nullable: true }
        province: { type: string, nullable: true }
        provinceCode: { type: string, nullable: true }
        county: { type: string, nullable: true }
        countyCode: { type: string, nullable: true }
        city: { type: string, nullable: true, description: City or municipality }
        municipality: { type: string, nullable: true }
        locality: { type: string, nullable: true }
        manualLocationFields: { type: array, items: { type: string }, description: Fields explicitly maintained by an administrator }
        geocodeProvider: { type: string, nullable: true }
        geocodeStatus: { type: string, enum: [ENRICHED, FAILED, NOT_CONFIGURED, SKIPPED_NO_CENTROID], nullable: true }
        geocodedAt: { type: string, format: date-time, nullable: true }
        provenance: { type: object, additionalProperties: true }
    LocationMetadata:
      type: object
      additionalProperties: false
      properties:
        continent: { type: string, nullable: true }
        continentCode: { type: string, nullable: true }
        country: { type: string, nullable: true }
        countryCode: { type: string, nullable: true }
        region: { type: string, nullable: true }
        regionCode: { type: string, nullable: true }
        subdivision: { type: string, nullable: true }
        subdivisionCode: { type: string, nullable: true }
        province: { type: string, nullable: true }
        provinceCode: { type: string, nullable: true }
        county: { type: string, nullable: true }
        countyCode: { type: string, nullable: true }
        city: { type: string, nullable: true }
        municipality: { type: string, nullable: true }
        locality: { type: string, nullable: true }
    LocationOptions:
      type: object
      required: [provider, continents]
      properties:
        provider: { type: string, example: BIGDATACLOUD }
        codeStandards: { type: object, additionalProperties: { type: string } }
        continents:
          type: array
          items:
            type: object
            required: [name, countries]
            properties:
              name: { type: string }
              code: { type: string, nullable: true }
              countries:
                type: array
                items:
                  type: object
                  required: [name, subdivisions]
                  properties:
                    name: { type: string }
                    code: { type: string, nullable: true }
                    subdivisions:
                      type: array
                      items:
                        type: object
                        required: [name, provinces]
                        properties:
                          name: { type: string }
                          code: { type: string, nullable: true }
                          provinces:
                            type: array
                            items:
                              type: object
                              required: [name]
                              properties:
                                name: { type: string }
                                code: { type: string, nullable: true }
    LocationMetadataEdit:
      type: object
      required: [location, editorId]
      properties:
        location:
          allOf:
            - $ref: '#/components/schemas/LocationMetadata'
          description: Stable GeoEntity location fields; null clears a manual value.
        manualFields:
          type: array
          items: { type: string, enum: [continent, country, region, subdivision, province, county, city, municipality, locality] }
          description: Fields that remain authoritative over reverse-geocoding refreshes.
        editorId: { type: string }
        note: { type: string }
    Activation:
      type: object
      required: [programmeSlug, entityId, operatorId, startedAt]
      properties:
        programmeSlug: { type: string }
        entityId: { type: string, format: uuid }
        operatorId: { type: string, format: uuid }
        startedAt: { type: string, format: date-time }
        entityType: { type: string }
        location: { type: object, additionalProperties: true }
    Qso:
      type: object
      required: [workedCallsign, timestamp]
      properties:
        workedCallsign: { type: string }
        timestamp: { type: string, format: date-time }
        band: { type: string }
        mode: { type: string }
        rst: { type: string }
        hunterId: { type: string, format: uuid }
        workedEntityId: { type: string, format: uuid }
        source: { type: string }
    AwardCondition:
      description: Programme-owned recursive AND/OR condition tree.
      oneOf:
        - type: object
          required: [kind, conditions]
          properties:
            kind: { type: string, enum: [AND, OR, ALL, ANY] }
            conditions: { type: array, items: { $ref: '#/components/schemas/AwardCondition' }, minItems: 1 }
        - type: object
          required: [kind, operator, value]
          properties:
            kind: { type: string, enum: [QSO_COUNT, ACTIVATION_COUNT, UNIQUE_CALLSIGNS, UNIQUE_ENTITIES, FIELD] }
            field: { type: string }
            operator: { type: string, enum: [GTE, GT, EQ, LTE, LT] }
            value: { type: number }
        - type: object
          required: [kind, values]
          properties:
            kind: { type: string, enum: [ENTITY_TYPE] }
            values: { type: array, items: { type: string }, minItems: 1 }
        - type: object
          required: [kind, condition]
          properties:
            kind: { type: string, enum: [NOT] }
            condition: { $ref: '#/components/schemas/AwardCondition' }
    AwardLevel:
      type: object
      required: [id, name, threshold]
      properties:
        id: { type: string }
        name: { type: string }
        threshold: { type: number, exclusiveMinimum: 0 }
    AwardAsset:
      type: object
      required: [kind, name, objectKey, mediaType, widthPx, heightPx]
      properties:
        assetId: { type: string, format: uuid }
        kind: { type: string, enum: [BACKGROUND, SIGNATURE] }
        name: { type: string }
        objectKey: { type: string }
        mediaType: { type: string, pattern: '^image/' }
        widthPx: { type: integer, minimum: 1 }
        heightPx: { type: integer, minimum: 1 }
        sha256: { type: string }
        bucket: { type: string }
    AwardTemplateElement:
      type: object
      required: [kind, x, y, width, height]
      properties:
        kind: { type: string, enum: [AWARD_NAME, CALLSIGN, PERSON_NAME, DATE_OBTAINED, MANAGER_NAME, MANAGER_SIGNATURE] }
        x: { type: number, minimum: 0, maximum: 1 }
        y: { type: number, minimum: 0, maximum: 1 }
        width: { type: number, exclusiveMinimum: 0, maximum: 1 }
        height: { type: number, exclusiveMinimum: 0, maximum: 1 }
        style: { type: object }
    AwardTemplate:
      type: object
      required: [elements]
      properties:
        elements: { type: array, minItems: 6, items: { $ref: '#/components/schemas/AwardTemplateElement' } }
    AwardPrintSpec:
      type: object
      properties:
        page: { type: string, enum: [A4, LETTER], default: A4 }
        orientation: { type: string, enum: [PORTRAIT, LANDSCAPE], default: PORTRAIT }
        dpi: { type: integer, minimum: 150, default: 300 }
    AwardDefinition:
      type: object
      required: [programmeSlug, code, name, category, condition, levels, backgroundAsset, template]
      properties:
        awardId: { type: string, format: uuid }
        programmeSlug: { type: string }
        code: { type: string }
        name: { type: string }
        description: { type: string }
        category: { type: string, enum: [HUNTER, ACTIVATOR] }
        achievementMetric: { type: string, enum: [QSO_COUNT, ACTIVATION_COUNT, UNIQUE_CALLSIGNS, UNIQUE_ENTITIES] }
        condition: { $ref: '#/components/schemas/AwardCondition' }
        levels: { type: array, minItems: 1, items: { $ref: '#/components/schemas/AwardLevel' } }
        backgroundAsset: { $ref: '#/components/schemas/AwardAsset' }
        printSpec: { $ref: '#/components/schemas/AwardPrintSpec' }
        template: { $ref: '#/components/schemas/AwardTemplate' }
    AwardReview:
      type: object
      required: [decision, reviewerId]
      properties:
        decision: { type: string, enum: [APPROVED, CHANGES_REQUESTED] }
        reviewerId: { type: string }
        note: { type: string }
    AwardPublication:
      type: object
      required: [effectiveFrom, publisherId]
      properties:
        effectiveFrom: { type: string, format: date-time }
        publisherId: { type: string }
    AwardEvaluationRequest:
      type: object
      required: [awardId, subjectId, facts]
      properties:
        awardId: { type: string, format: uuid }
        subjectId: { type: string, format: uuid }
        facts: { type: object, additionalProperties: true }
    AwardProgressRequest:
      type: object
      required: [awardId, subjectId]
      properties:
        awardId: { type: string, format: uuid }
        subjectId: { type: string, format: uuid }
    AwardRequest:
      type: object
      required: [awardId, levelId, subjectId, callsign, personName]
      properties:
        awardId: { type: string, format: uuid }
        levelId: { type: string }
        subjectId: { type: string, format: uuid }
        callsign: { type: string }
        personName: { type: string }
        facts: { type: object, additionalProperties: true, description: Optional facts for trusted service/admin callers; participant requests use server-side activity facts. }
    AwardIssuanceRequest:
      type: object
      required: [managerName, signatureAssetId]
      properties:
        managerName: { type: string }
        signatureAssetId: { type: string, format: uuid }
        dateObtained: { type: string, format: date-time }
    AssetContent:
      type: object
      required: [contentBase64]
      properties:
        contentBase64: { type: string, contentEncoding: base64 }
